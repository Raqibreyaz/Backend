<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>All Users</title>
</head>

<body>
    <div class="p-[2vw] bg-zinc-800 h-screen w-screen text-white text-[1.4vw]">
        <a href="/" class="mb-3 inline-block text-blue-400 ">Home</a>
        <h1 class="text-[2.5vw] mb-4 capitalize">
            <%= users.length ? 'all users' :'no users yet' %>
        </h1>
        <div class="flex flex-wrap gap-2">

            <% users.forEach(function(user){ %>
                <div
                    class="w-[200px] h-[300px] border border-gray-500 p-[1vw] flex flex-col justify-between rounded-[10px]">
                    <div class="bg-gray-400 w-full h-[60%] rounded-[5%] overflow-hidden">
                        <img src="<%= user.imageUrl %>" alt="" class="h-full w-full">
                    </div>
                    <div>
                        <div class="text-[2vw]">
                            <%= user.name %>
                        </div>
                        <div class="text-gray-500">
                            <%= user.email %>
                        </div>
                    </div>
                    <div class="capitalize flex items-center gap-[1vw]">
                        <a href="/update/<%= user._id %>" class="text-zinc-200">edit user</a>
                        <a href="/delete/<%= user._id %>" class="text-red-500">delete user</a>
                    </div>
                </div>
                <% })%>

        </div>
    </div>
</body>

</html>